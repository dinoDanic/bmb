# Commands

Use `pnpm` (lockfile present) or `npm` consistently.
- `pnpm dev` runs the Next.js dev server at `http://localhost:3000`.
- `pnpm build` creates a production build.
- `pnpm start` runs the production server from `.next/`.
- `pnpm lint` runs ESLint (`eslint.config.mjs`).
