# Commit & PR

Commit history is minimal (`init`, `Initial commit...`), so no formal convention is established.
Use clear, imperative messages (e.g., "Add hero CTA copy").

For PRs, include:
- A short summary of changes.
- Linked issue or context, if applicable.
- Screenshots for UI changes.
