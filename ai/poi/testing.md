# Testing

No test framework is configured yet (no `__tests__`, `*.test.*`, or `*.spec.*` files).
If adding tests, introduce a framework (e.g., Vitest or Jest) and align naming to `*.test.ts(x)`.
Document new test commands in `ai/commands.md`.
